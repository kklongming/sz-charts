<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    <!-- 引入 lib/echarts.custom.min.js -->
    {% for src in echartsScripts %}
    <script src="{{ src }}"></script>
    {% endfor %}
</head>
<body>
    <div id="main" style="{{ sizeStyle }}"></div>
</body>
</html>
<script>
// 基于准备好的dom，初始化echarts实例
var myChart = echarts.init(document.getElementById('main'), '{{ theme }}');
var options = {{ options }};
myChart.setOption(options);
</script>